{
  "scripts": {
    "test": "cypress run --reporter mochawesome --reporter-options reportDir=cypress/reports,overwrite=false,html=false,json=true",
    "merge:reports": "mochawesome-merge cypress/reports/*.json > mochawesome.json",
    "generate:report": "marge mochawesome.json --reportDir cypress/reports --inline"
  },
  "devDependencies": {
    "cypress": "^14.4.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  }
}
